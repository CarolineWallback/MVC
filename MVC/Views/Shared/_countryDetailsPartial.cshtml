@model Country

@if (Model != null)
{
    <div class="row bg-light text-bg-light p-1">
        <div class="col">
            <h5>@Model.CountryName</h5>
        </div>
        <div class="col">
           @* <a type="submit" class="btn btn-danger btn-sm" asp-controller="City" asp-action="DeleteCity" asp-route-id=@Model.CityId>Delete City</a>*@
        </div>
    </div>
}


